/**
 * Types de la base Supabase.
 *
 * Placeholder pour PR0 — sera remplacé en PR1 par la sortie de
 * `supabase gen types typescript` (ou l'outil MCP generate_typescript_types)
 * une fois le schéma migré.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
